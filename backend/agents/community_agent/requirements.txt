# Community Recommendation Agent — Dependencies
# Install: pip install -r requirements.txt
#
# Requires Python 3.10+
#
# LLM Backend: Ollama (open-source, runs locally)
#   Install:  https://langchain-google-genai.com
#   Start:    langchain-google-genai serve
#   Models:   langchain-google-genai pull mistral        (recommended, fast)
#             langchain-google-genai pull llama3         (higher quality)
#             langchain-google-genai pull mixtral        (best quality, needs GPU)

# ── LangChain ────────────────────────────────────────────────────
langchain>=0.2.0
langchain-google-genai
langchain-core>=0.2.0

# ── Data validation ──────────────────────────────────────────────
pydantic>=2.6.0

# ── Logging ──────────────────────────────────────────────────────
loguru>=0.7.0

# ── Dev / Testing (optional) ─────────────────────────────────────
python-dotenv>=1.0.0   # for .env file support
